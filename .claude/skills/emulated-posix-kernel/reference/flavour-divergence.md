# Where each measured kernel fact lives

Every Linux/Darwin divergence PawPrint models is already written down **as a
test**, with its rows in one place and an assertion that keeps them true. This
file is the index, not a second copy: a table restated here could drift from the
test that enforces it, and the restatement would be the version nobody runs.

So: to learn what a kernel answers, open the test named below and read its rows.
To change what PawPrint answers, change the test first.

## The index

| fact | owned by |
| --- | --- |
| `NAME_MAX`, `PATH_MAX`, the symlink limit | `TestVirtualFileSystemAgainstHost`: `pathLimits states this kernel's real symlink limit exactly`, `…real PATH_MAX exactly`, `pathLimits agrees with this kernel about which names are too long` |
| **Darwin's `NAME_MAX` is two limits**: 255 UTF-16 code units for a strictly-valid UTF-8 name, 765 raw bytes for anything else. Linux's is 255 bytes for every name | `TestUnixPathBytes`: `Darwin's NAME_MAX flips exactly where it was measured to` (the bisection per repeated unit), `Darwin counts strictly-valid UTF-8 in code units and anything else in bytes`, `Linux counts every name in bytes`. Probes in `docs/plans/2026-09-20-unix-path-bytes/` |
| **Which names a binding admits, and where the refusal falls.** Darwin answers EILSEQ for binding a name that is not valid UTF-8 — `open(O_CREAT)`, `mkdir`, and `rename`'s destination — and it is the *last* refusal of each: `NAME_MAX`, then the parent's write bit, then the encoding, so `mkdir("ro/" + 300 × 0xFF)` is EACCES in an unwritable `ro` and EILSEQ in a writable one. Every other refusal wins too: ENOENT for a trailing separator, a missing source or a removed parent, EINVAL for a move into the source's own subtree. Looking such a name up is plain ENOENT. Linux binds any NUL-free bytes. APFS also refuses some *valid* UTF-8, which the model admits: see `docs/divergences.md` | `BindingProbes.rows`, each with Darwin's measured answer, asserted by `TestUnixPathBytes`: `Darwin refuses to bind a name that is not UTF-8, after every other refusal`, and put to the live kernel by `TestBindingAgainstHost`: `every probe row agrees with this host's kernel`. The removed-parent rows are `nothing is bound inside a removed directory, whatever the name`. Wiring, and EILSEQ in both numberings a guest reads: `sourcesImpure/BindNonUtf8Wiring{Linux,Darwin}Seeded.cs`. Probes: `darwin-name-max-vs-permission.c`, `darwin-eilseq-is-last.c` |
| names and symlink targets are bytes: `readdir`, `getcwd` and `readlink` hand back exactly the bytes that were bound, on both flavours; a symlink *target* is never checked for encoding, even on APFS | `TestUnixPathBytes`: `getcwd reports a directory name that is not UTF-8 byte for byte`, `readdir hands back names that are not UTF-8 byte for byte`, `readlink and lstat report a seeded target byte for byte` |
| symlink splicing, and every path-resolution shape | `TestVirtualFileSystemAgainstHost`: `the model splices symlink targets exactly as this kernel does`, `the model resolves every probe path exactly as this kernel does`; shape-by-shape unit rows in `TestVirtualFileSystem` |
| the directory *search* bit during a walk — which is **unanimous**, and is here because the corpus that pins it is shared with the rows above | the same two host-oracle tests, whose corpus carries unsearchable directories (`ns`, `cls677`, `cls500`); `TestWalkSearchPermission` for the precedence rows, the owner-triple pair and the privileged bypass; `sourcesImpure/SearchPermission{,Root,Cwd}Seeded.cs` for the wiring |
| `open(O_CREAT)` — trailing separators, existing directories, the root, the mode mask | `TestVirtualFileSystemAgainstHost`: `a creating open decides exactly as this kernel does`, kept honest by `the creating-open corpus reaches every verdict this kernel can give` |
| truncation and the set-ID bits | `TestVirtualFileSystem`: `truncation strips the set-ID bits only on Linux, and only for a non-root caller` — the measured table as octal literals, deliberately not computed from `afterTruncation`, and covering root as well as non-root. Live host comparison in `TestVirtualFileSystemAgainstHost`: `truncation moves a file's set-ID bits exactly as this kernel does`, `truncation leaves a file's ordinary permission bits alone` |
| `ftruncate` and `O_TRUNC` end-to-end, including which descriptor kinds refuse | `sourcesPure/TruncateSeeded.cs`, plus `sourcesImpure/TruncateWiring{Linux,Darwin}Seeded.cs` for the wiring |
| `mkdir(2)` — what a trailing separator costs on the final component, the mode mask, and set-group-ID inheritance | `TestVirtualFileSystemAgainstHost`: `mkdir decides exactly as this kernel does`, kept honest by `the mkdir corpus reaches every verdict this kernel can give`; the mode tables live against the host in `a new directory keeps exactly the mode bits this kernel keeps` and `a new directory inherits set-group-ID exactly as this kernel does`. `TestMkDirRules` carries the same tables as octal literals plus the rows the host oracle structurally cannot reach: the flavour it is not running on, `mkdir("/")`, the permission bits, and *which name* a creation binds (Darwin's `mkdir("dang/")` binds the link's target) |
| `mkdir` end-to-end, including the uid rule in both directions | `sourcesPure/MkDirSeeded.cs`, plus `sourcesImpure/MkDirWiring{Linux,Darwin}Seeded.cs` for the wiring and `sourcesImpure/MkDirPrivilegedSeeded.cs` for uid 0 |
| `unlink(2)` — which errno each refusal carries, the *order* the checks are made in, and whether a trailing separator resolves the final symlink | `TestVirtualFileSystemAgainstHost`: `unlink decides exactly as this kernel does, and destroys the same thing`, which compares a before/after **tree delta** rather than an errno, kept honest by `the unlink corpus reaches every verdict this kernel can give`. `TestUnlinkRules` carries both columns as measured rows, including the ones the host oracle structurally cannot reach: its stand-in root is an ordinary directory, so Darwin's EBUSY-for-the-mount-root arm is invisible there |
| `unlink` end-to-end, including the uid rule | `sourcesPure/UnlinkSeeded.cs` for what the flavours agree on, `sourcesImpure/UnlinkWiring{Linux,Darwin}Seeded.cs` for the wiring, `sourcesImpure/UnlinkReapSeeded.cs` for the inode-lifetime rule (asserted on the terminal state, since no guest can read it) |
| `opendir(3)` — which errno each refusal carries and the order the checks are made in, on which the two kernels **agree in every row measured**, which is why `OpenDirRules.verdict` takes no flavour | `TestOpenDirRules` carries the rows as literals, including the two the host oracle structurally cannot reach: the real root (`/`, `/.`, `/..` — its stand-in root is an ordinary directory) and what uid 0 sees. `TestVirtualFileSystemAgainstHost`: `opendir decides exactly as this kernel does`, kept honest by `the opendir corpus reaches every verdict this kernel can give` |
| **`opendir` demands the *read* bit, not the search bit** — the first place in this codebase where the two come apart. A `0o111` directory (search, no read) is EACCES on both; a `0o444` one (read, no search) opens and lists every name | `TestOpenDirRules`' `dr`/`dw` pair, which exists to be asymmetric: a verdict demanding search would refuse one and admit the other and every other row would still pass. The host oracle's `ns` (0o666) is the same row against a live kernel |
| **Being a file beats being unreadable**: `opendir` on a *mode-0000* regular file is ENOTDIR, not EACCES, on both — with and without a trailing separator, and through a symlink to one | `TestOpenDirRules`: `being a file beats being unreadable` |
| `readdir(3)`'s `d_type` for each inode kind, and that `.` and `..` are yielded by the *stream* rather than stored in the directory | `sourcesPure/EnumerateSeeded.cs`, which walks the raw shim. Not implied by the `GetFiles`/`GetDirectories` split: `FileSystemEntry` consults `InodeType` only when it is not `DT_UNKNOWN` and falls back to `stat` otherwise |
| **`.` and `..` have no fixed position, and neither does anything else.** Measured, a directory holding one name `z` enumerates as `. .. z` on APFS and in a Linux container, and as `z .. .` on CI's ext4 — dots last, and in the other order. The two disagreeing machines are both ext4, so this is not a flavour split. On the two filesystems the library models the order is an exact rule at the syscall level: tmpfs yields `.`, `..`, then the newest link first; APFS yields `.`, `..`, then by `(~crc32c(UTF-32LE(casefold(NFD(name))))) & 0x3FFFFF` with ties by name bytes (1055 of 1055 listings reproduced). The model keeps its own order | the measured table in `docs/divergences.md`, "Directory enumeration order is the model's own, not any kernel's". Nothing asserts an order: `EnumerateSeeded.cs` sorts the shim's own walk, dots included. An earlier version asserted dots-first and passed on two machines before CI falsified it — and PawPrint's own stream now puts the dots **last**, the less convenient of the two measured orders, so a guest making that assumption fails deterministically instead of on whichever machine runs it |
| **`DirectoryEntry.NameLength` diverges**: `-1` on Linux, the name's length in bytes on Darwin. `ConvertDirent` copies `d_namlen` under `HAVE_DIRENT_NAME_LEN` and writes the sentinel otherwise; established by *compiling*, since glibc's `struct dirent` has no such member. Invisible to managed code, which decodes both to the same string | `DirectoryEntryPal.directoryEntryNameLength` (PawPrint), asserted end-to-end by `sourcesImpure/EnumerateWiring{Linux,Darwin}Seeded.cs` through a hand-rolled `[DllImport]`. Their names are 1, 2 and 6 bytes long over 1, 1 and 2 characters, so a `String.Length` rule agrees on the first only |
| directory enumeration end-to-end, including `Directory.Delete(recursive: true)` and a stream over an `rmdir`'d directory | `sourcesPure/EnumerateSeeded.cs`. What a mutation mid-stream does **is** a kernel fact at the syscall level, with an exact rule per filesystem (measured over 1280 scripts each; the table is in `docs/divergences.md`), which libc's buffer then hides until it refills. PawPrint's choice, a cursor by name, is recorded there and pinned by `TestDirectoryEnumeration` and `TestDirectoryDescription` |
| **The directory position lives on the open file description**: a `dup` shares it, a second `open` does not, `lseek(fd, 0, SEEK_SET)` rewinds, and any `SEEK_SET` offset `>= 0` is accepted with a regular file's `SEEK_CUR` arithmetic (EINVAL below zero; past `INT64_MAX` EINVAL on Linux, EOVERFLOW on Darwin). The value mid-scan is the filesystem's own cookie (tmpfs: the next entry's offset; APFS: a per-directory sequence number over an entry count), `0x7FFFFFFF` at the end on both, so the model answers `SEEK_CUR` only at the start and after an `lseek` | `TestDirectoryDescription` (the laws, both flavours), `TestDirectoryReadAgainstHost`: `a description yields every entry once between rewinds, shared across dup, as this host's does`. Probes: `docs/plans/2026-08-23-posix-kernel-extraction/directory-layout.c`, `directory-descriptors.c`, measured on Linux 6.18.5 (arm64, tmpfs) and Darwin 27.0.0 (APFS) on 2026-09-26 |
| **`getdents` records**: `d_ino` equals `lstat`'s `st_ino` for every entry, `.` (the directory) and `..` (its parent) included; `d_type` is exact for every kind on tmpfs and APFS, never `DT_UNKNOWN`. Layouts: Linux `linux_dirent64` (`d_type` @18, name @19, `reclen = align8(20 + namlen)`); Darwin `struct direntry` (`d_namlen` @18, `d_type` @20, name @21, `reclen = (namlen + 32) & ~7`, `d_seekoff` always 0 on APFS). A buffer too small for the next record is EINVAL on Linux; on Darwin anything under 64 bytes is EINVAL, and otherwise it is a false end-of-directory that consumes nothing. Darwin reserves a buffer's last 4 bytes as an end-of-directory flags word from 1024 bytes up | `TestDirectoryReadAgainstHost` for `d_ino` and `d_type`; the layout rows are not yet modelled (no raw `getdents` entry point). Probes: `directory-layout.c`, `directory-eofflag.c` |
| **Reading a directory descriptor that cannot be read**: a directory `rmdir` has removed is ENOENT on Linux at every position, and 0 on Darwin (EAGAIN at an `lseek` offset of 2^62, which is why an `lseek`-chosen position is refused there). A non-directory is ENOTDIR on Linux for every kind; on Darwin EINVAL for a readable regular file, EBADF for a write-only one, and ENOTSUP for a pipe, socket or kqueue | `TestDirectoryDescription`, and `TestDirectoryReadAgainstHost`: `every descriptor that is not a directory answers this host's errno`. Probe: `directory-descriptors.c` |
| `rmdir(2)` — which errno each *navigation* carries, the order the checks are made in, whether a trailing separator resolves the final symlink, and whether the removed directory's own `ctime` moves | `TestRmDirRules` carries both columns as measured rows, including the root-navigation arms the host oracle structurally cannot reach. The two flavours split on *what the root is*: Linux specialises the path that consumed no component (`/` is EBUSY, `/.` is EINVAL) where Darwin specialises the root inode (`/` is EISDIR, `/.` is EBUSY). Below the root they split on `..`: ENOTEMPTY on Linux, EINVAL on Darwin 27 (Darwin 25 said ENOTEMPTY). `TestVirtualFileSystemAgainstHost`: `rmdir decides exactly as this kernel does, and destroys the same thing` |
| **`rmdir` destroys different objects on the two flavours.** With `ld -> d` and `d` empty, `rmdir("ld/")` removes `d` on Darwin and is ENOTDIR on Linux — the divergence `Resolution.FinalSymlinkFollowed` warns about, and the reason that fixture compares tree deltas rather than errnos | The host oracle's `lsub` corpus row, and `sourcesImpure/RmDirWiringDarwinSeeded.cs`, which asserts the removal *and* that the link itself survives |
| `rmdir` end-to-end, and what an orphaned directory can still do | `sourcesPure/RmDirSeeded.cs` for what the flavours agree on, `sourcesImpure/RmDirWiring{Linux,Darwin}Seeded.cs` for the wiring, `sourcesImpure/RmDirOrphan{Linux,Darwin}Seeded.cs` for standing in a removed directory. The ancestor pinning and the cascading reap are asserted on the terminal state, since no guest can read them |
| `getcwd(3)` from a *removed* current directory — EINVAL on both at size 0, ENOENT on Linux at every other size, and on Darwin ERANGE at size 1 with ENOENT from 2 up (a minimum, not a comparison against the stale path) | `SimulatedUnixPlatform.getCwdOrphanAnswer`, asserted end-to-end by the last rows of each `RmDirOrphan*Seeded.cs` — including the size-5 row, which is what distinguishes the minimum from a path-length check. Only reachable since `rmdir` could orphan a current directory |
| `fstatfs` magic numbers, and which flavour can mount what | `TestFileSystemType`: `each filesystem's magic number is the one CoreLib reads it back as` (a `DllImport` of the PAL function itself), `every flavour's default is a filesystem that flavour can mount`, `a filesystem the flavour could not mount is refused` |
| **A directory's `st_size`, and `lseek(dir, off, SEEK_END)`, are per *filesystem*, not per flavour.** tmpfs (Linux): `40 + 20 × names`, and `SEEK_END` is EINVAL for every offset. APFS (Darwin): `64 + 32 × names`, and `SEEK_END` is a regular file's arithmetic over that size. Neither moves with name length, entry kind, history, or a subdirectory's contents. NFS is the server's own filesystem's answer, so it is unmeasured and refused (`StatRefusal.NfsDirectorySize`, `FStatRefusal.NfsDirectorySize`, `LSeekRefusal.DirectoryEnd`) | `TestDirectorySize`: `a directory's size follows its entry count through any history` and `…out to 5000 names` (the rule as literals), `a directory's size and SEEK_END agree with this host's through any history` (`/dev/shm` on Linux, the temporary directory on macOS; skipped where that is not tmpfs or APFS), and the per-offset `SEEK_END` rows. Wiring: `TestLSeek`'s three `SEEK_END on a … directory` tests, `TestStatNfsDirectory`, and `sourcesImpure/StatFieldsSeeded.cs`. Measured 2026-09-23; the sweep is in the comment on `EmulatedFileSystemType.directorySize` |
| `lseek` whence handling per flavour | `TestLSeek`: `the refused whence is named per the simulated platform`, `Darwin decides seekability before the whence is validated`, `Darwin reports EOVERFLOW where Linux reports EINVAL` |
| **The facts the architecture decides, not the flavour**: `sizeof(struct epoll_event)` is 12 on x86-64 (packed) and 16 on aarch64, so `epoll_wait`'s largest `maxevents` is 178956970 and 134217727 and its buffer screen's stride is 12 and 16; Linux's page size is 4096 on both, so `getrandom`'s cap (`MAX_RW_COUNT`, `INT_MAX` rounded down to a page) is 0x7FFFF000 on both; Darwin arm64's pages are 16384; `TASK_SIZE_MAX` is 2^47 − 4096 or 2^56 − 4096 on x86-64 (four- or five-level paging) and 2^48 on 48-bit-VA aarch64. `SimulatedUnixPlatform.create` admits only Linux x86-64 and aarch64 with 4 KiB pages and Darwin arm64 with 16 KiB, and a machine's address limit must be its architecture's. Rosetta (`container --arch amd64`) is **not** an x86-64 kernel here: it passes `maxevents` through to the aarch64 kernel and screens no buffer | `TestArchitectureFacts` (the measured table as literals: what `create` admits, epoll's constants, the page sizes and `getrandom`'s cap, which limits belong to which architecture, and `UserBufferCheckNotOfPlatform`), `TestArchitectureAgainstHost` (page size, epoll's bound and screened stride against the host), `TestSocketWait`'s per-architecture rows. Probes: `docs/plans/2026-08-23-posix-kernel-extraction/architecture-facts-{linux,darwin}.c`, measured 2026-09-26 on Linux 6.18.5 aarch64, on Debian's 6.12 x86-64 kernel under QEMU, and on Darwin 27.0.0 arm64 |
| **`socket(2)`'s arguments**: which (domain, type, protocol) each kernel creates, the errno for the rest, and the check order. Linux: invalid type-flag bits EINVAL, then a family outside [0, 46) EAFNOSUPPORT, then a type above 10 EINVAL, then per family (`AF_UNIX` checks the protocol, accepting 0 and `PF_UNIX`, before the type, and makes `SOCK_RAW` a `SOCK_DGRAM` socket; `AF_INET`/`AF_INET6` check the protocol against [0, 263) first, then answer ESOCKTNOSUPPORT for a type no protocol provides and EPROTONOSUPPORT for a protocol the type lacks). Darwin: a lookup in the domain's protocol switch, EPROTOTYPE for a protocol it has under another type, else EPROTONOSUPPORT, EAFNOSUPPORT for a family it lacks. Refused, as depending on what is not modelled: internet raw sockets and Linux's `AF_INET` `SOCK_PACKET` (privilege), ICMP datagram sockets (Darwin creates them; Linux's `net.ipv4.ping_group_range`), SCTP/MPTCP/SMC/L2TP/UDP-Lite and every internet `SOCK_SEQPACKET` on Linux (how the kernel was built), and every other family the kernel has | `TestSocketSyscall` against `socketSyscall/{linux,linux-root,darwin}.tsv` (392,736 calls each: domains -2..63 and nine beyond, types 0..15 with and without flag bits, protocols -2..300 and seven beyond), measured on Linux 6.18.5 aarch64 (an x86-64 build under Rosetta agreed, but that is the same aarch64 kernel; the code path is architecture-independent, and CI's x86-64 kernel runs the host test) at euid 1000 and root and on Darwin 27.0.0 at euid 501 on 2026-09-26 by `docs/plans/2026-08-23-posix-kernel-extraction/socket-arguments.c`; `TestSocketSyscallAgainstHost` re-runs every answered call on the host. The runtime's argument screens before it: `TestSocketCreation` against `WoofWare.PawPrint.Test/socketMatrix/{linux,darwin}.tsv`, whose answers are privilege-dependent and which no single machine can measure both columns of |
| `accept(2)`'s error matrix — EAGAIN / EINVAL / EOPNOTSUPP / ENOTSOCK / EBADF and their check ordering, on which the kernels agree in classification and differ only in numbering | `sourcesPure/SocketAccept.cs` (PAL codes, differential), `sourcesImpure/SocketAccept{Linux,Darwin}.cs` (raw numbers) |
| **`accept(2)`'s O_NONBLOCK inheritance**: the accepted socket inherits the listening *description*'s flag on Darwin and does not on Linux. Invisible to a guest, because `SystemNative_Accept` clears it under `#if !defined(__linux__)` — "Our socket code expects new socket to be in blocking mode by default" — which is why the kernel fact and the PAL's normalisation are modelled in different places | `SimulatedUnixPlatform.acceptedSocketInheritsNonBlocking`, measured by `docs/plans/2026-08-23-posix-kernel-extraction/accept-inherits-nonblock.c` on Linux 6.18.5 and Darwin 25.6.0; asserted by `TestAccept`'s three inheritance rows, which is the only tier that can see it |
| `ENOTSOCK` from a socket syscall on a non-socket descriptor, measured per entry point (`accept`/`bind`/`listen`/`getsockname` × file/event port/pipe) | the same three guests: `SocketAccept.cs` rows 35-40, `SocketAccept{Linux,Darwin}.cs` rows 19-27 |
| `epoll_ctl`'s error matrix and check ordering — EFAULT (every op but DEL, first of all) / EBADF port then target / EPERM (file or directory) / EINVAL (not a port, or the same description) / EINVAL (EPOLLEXCLUSIVE on MOD, on an epoll target, or beside any bit outside IN\|OUT\|ERR\|HUP\|WAKEUP\|ET\|EXCLUSIVE) / EEXIST / ENOENT / EINVAL (unrecognised op), no event bit rejected outright; the (fd, description) registration key; nesting (ELOOP on a cycle and past four ports, refused by the model); EPOLLWAKEUP silently cleared without `CAP_BLOCK_SUSPEND`; and where kqueue diverges (silent-replace ADD, file targets succeeding, dead-target DEL answering ENOENT not EBADF) | `TestEpollCtl` against `epollCtl/linux-ladder.txt` (every cell of the probe's ladder, checked in as data) and the EXCL rule as a property; probe `docs/plans/2026-08-23-posix-kernel-extraction/epoll-ctl.c`, Linux 6.18.5 aarch64 (an x86-64 userland under Rosetta agreed; no x86-64 kernel measured), 2026-09-26; `sourcesPure/SocketEventRegistration.cs` (the rows the flavours agree on, differential), `sourcesImpure/SocketEventRegistrationLinux.cs` (the epoll-only rows; the Darwin kernel arm is a refusal, so no Darwin twin exists) |
| `connect(2)`'s ladder — blocking loopback success, EINPROGRESS even on loopback, ECONNREFUSED, and where the flavours diverge (listening-socket connect EISCONN/EOPNOTSUPP, after a refusal: Linux delivers it once then resets, Darwin 27 never delivers it and answers EISCONN (Darwin 25 delivered once then answered EINVAL), bound-not-listening RST/SYN-drop, AF_UNSPEC accepted/refused, oversized sockaddr, accept-queue capacity backlog+1/backlog) — plus `accept(2)`'s dequeue: FIFO order, peer/own addresses, the accepted fd blocking through the PAL | `sourcesPure/SocketConnect.cs` (the agreement rows, differential), `sourcesImpure/SocketConnectLinux.cs` / `SocketConnectDarwin.cs` (the divergent rows and raw errnos, each confirmed on its real runtime); `TestEmulatedKernelSockets` (connection identities, queue write-back, the close sweep, capacity boundaries) |
| the readiness delivery — per-phase epoll levels (idle stream OUT\|HUP, datagram OUT with no HUP, listener IN iff queue nonempty, established OUT, pending refusal 0x201d, pipe ends), reporting as the raw level ∩ (events ∪ {ERR, HUP}) for every one of 21024 masks per state, the level being poll's own (RDNORM with IN, WRNORM with OUT, WRBAND with OUT on UDP and AF_UNIX); a listener's data-ready wake keyed IN\|PRI\|RDNORM\|RDBAND rather than by the level, the ready list's order (edge arrival; re-signal immobility; ADD/MOD-of-ready at ADD/MOD time; same-socket dup ties LIFO by registration, MOD not moving them), which operations signal (queue push, connect resolution, refusal reset — not bind, not the SUCCESS report, not UDP re-target), truncation, and the PAL's EPOLLHUP→IN\|OUT fold at delivery | probes in `docs/plans/2026-08-21-socket-readiness-wake/`; `sourcesPure/SocketEventDelivery.cs` (portable rows plus the wake, differential), `sourcesImpure/SocketEventDeliveryLinux.cs` (exact masks and order; Linux-flavour only, since Darwin's registration arm refuses), `TestSocketEventDelivery` (the ready list itself, one test per measured row, and the wake key per bit); `TestEpollCtl` (the reported masks, from `epoll-ctl.c`'s REPORT section) |
| **signal numbering**: which signo each named signal has (six differ — `SIGUSR1` 10/30, `SIGUSR2` 12/31, `SIGCHLD` 17/20, `SIGCONT` 18/19, `SIGTSTP` 20/18, `SIGURG` 23/16), the kernel's highest signo (64, glibc's `SIGRTMAX`; 31, one below Darwin's `NSIG`), which numbers `sigaction(2)` refuses (SIGKILL and SIGSTOP on both — 9/19 and 9/17 — plus glibc's reserved 32 and 33), and the default disposition of the unnamed ones (Linux's 29 is `SIGIO` and terminates; Darwin's 23 is `SIGIO` and 29 is `SIGINFO`, both discarded). Two PAL facts sit beside these: `GetSignalMax()` is `SIGRTMAX` or else `NSIG`, so on Darwin the shim admits a 32 the kernel refuses; and `HandleNonCanceledPosixSignal` has explicit no-op arms for seven signals only, so Darwin's discarded `SIGIO`/`SIGINFO` take its `default:` arm and lose their native handler | `TestSignal` carries both columns as literals; `TestSignalAgainstHost` checks the host's column against its own `kill -l`; `TestPosixSignalPal` checks `platformSignalNumber` against the host's `SystemNative_GetPlatformSignalNumber` over every input in `[-100, 100]`. Wiring: `sourcesImpure/SystemNativeGetPlatformSignalNumber{Linux,Darwin}.cs`, `sourcesImpure/SystemNativePosixSignalHandling{Linux,Darwin}.cs`, and `TestSignalTermination` for the identity a terminating signo carries |
| **errno numbering**: every error either `<errno.h>` names. The two agree only on 1..34 except 11 (`EAGAIN` and `EDEADLK` are transposed: 11/35 and 35/11); everything else was numbered independently. Linux assigns 1..133 but 41 and 58, and has 44 errors Darwin lacks (`ECHRNG`..`EHWPOISON`, the `ENOKEY` family); Darwin assigns 1..107 (`ENOTCAPABLE` 107 is in the macOS 26 SDK and Darwin 27's libc, not the 14.4 SDK) and has 19 Linux lacks (`EAUTH`, `ENOATTR`, `EQFULL`, the RPC and Mach-O errors). Aliases: `EWOULDBLOCK` = `EAGAIN` on both, `EDEADLOCK` = `EDEADLK` on Linux, and Linux's `ENOTSUP` = `EOPNOTSUPP` (95) where Darwin has two (45, 102). The shim's `ConvertErrorPlatformToPal` answers `ENONSTANDARD` for every number whose error `Interop.Error` has no member for, and for every unassigned or negative number | `ErrnoHeaders` transcribes both headers; `TestUnixError` holds the table to it, and checks it against this host's `<errno.h>` and `strerror`. `TestUnixErrorPal`: `ofRawErrnoUnder answers what each flavour's shim was measured to` (both flavours, [-300, 4096]) and `…what this host's shim does`. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/errno-table.sh` |
| **`kill(2)`'s arguments**: the null signal answers 0; a number that is neither 0 nor a signal (anything outside 1..64 on Linux, which includes glibc's reserved 32 and 33, or 1..31 on Darwin) is EINVAL. The check order diverges: Linux resolves the target first, so a pid naming no process (or `INT_MIN`) is ESRCH whatever the number, where Darwin checks the number first and says EINVAL. So only the calling process is answered, and every other target is refused before the number is read. **Which terminating signals a CoreCLR process does not die of**, which is the runtime's fact rather than the kernel's: with nothing registered, a self-sent ILL, TRAP, ABRT, BUS, FPE, SEGV, PIPE, 33 and 34 on Linux, and ILL, ABRT, FPE, BUS, SEGV, PIPE and SIGUSR1 on Darwin; every other terminating signal kills it as the default says. The process survives each of them except SIGTRAP on Linux, which depends on the CPU: survived on aarch64, and fatal with **SIGILL** (132) on x86-64, because the runtime treats a kill-sent SIGTRAP as a breakpoint and backs the PC up by x86's one-byte `int3`. PawPrint refuses to generate those | `TestUnixSignal` carries the kernel rows as literals and property-tests the rest. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/kill-arguments.c`, measured on Linux 6.18.5 (arm64) and Darwin 25.6.0 on 2026-09-24. The runtime's column was also measured on Linux x86-64, under Rosetta (`container run --arch amd64`) and on CI's ubuntu runner, which agree: `StartupSignalDispositions` (comment), `TestNativeLibc` (both columns as literals), `TestStartupSignalDispositions` (the host's column against the real runtime) |
| **`clock_gettime` clock ids**: the numbering is each flavour's own (`CLOCK_MONOTONIC` is 1 on Linux, 6 on Darwin); Linux answers 0..9 and 11 plus some negative (CPU-time) ids and is EINVAL for 10, 12..15 and 24 up; its auxiliary clocks 16..23 depend on the kernel's build (EINVAL without `CONFIG_POSIX_AUX_CLOCKS` in the `container` VM, ENODEV on GitHub's ubuntu-24.04 runner until enabled), so the model refuses them. Darwin answers 0, 4, 5, 6, 8, 9, 12, 16 and is EINVAL for everything else, negatives included; no other id fails with any other errno. **Granularity**: Darwin's `CLOCK_REALTIME` and `CLOCK_MONOTONIC` report whole microseconds while its APFS inode stamps carry nanoseconds; every Linux clock, `_COARSE` included, carries nanosecond digits | `TestClock`: `each clock id reads the clock the measured table says, on each flavour`, `every answered clock reads exactly what its flavour reports`; `TestClockAgainstHost` sweeps [-4096, 4096] against the live host for the EINVAL split and the microsecond split. Probes (2026-09-23) swept the whole int range on a stride; see the comment above `UnixClock.decode` |
| **`setsockopt`/`getsockopt` of `SO_REUSEADDR`**: the numbering (`SOL_SOCKET` 1/`0xffff`, `SO_REUSEADDR` 2/4); the ladder, where Darwin screens a null value before the descriptor and Linux reads the length as a signed `int`; Darwin's EINVAL for any set on a socket whose connection was refused; what reads back (1 on Linux, the option's bit 4 on Darwin, for any non-zero value set); how much `getsockopt` copies and reports (the smaller of the declared length and `sizeof(int)`, except that Darwin reports 0 for a null value buffer); and that a later `bind`/`listen` reads every socket's flag as it stands then, not as it stood at that socket's bind | `TestSockOpt` carries both columns as literals; `TestSockOptAgainstHost` puts random inputs to the host kernel and the model. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/sockopt.c` |
| **the entropy syscall exists on one flavour each**: `getrandom(2)` on Linux, `getentropy(2)` on Darwin, and each refuses the other's. `getrandom` answers EINVAL for an unknown flag bit or `GRND_RANDOM\|GRND_INSECURE` ahead of every other check, clamps its count to `MAX_RW_COUNT` (0x7FFFF000) *before* the buffer screen, and is otherwise screened like `read`. `getentropy` answers EINVAL (not EIO) above 256 bytes, ahead of the buffer, and a zero-length call returns 0 at any address | `TestEntropyAgainstHost` against the live kernel, one fixture per flavour, plus `getrandom's largest transfer is this kernel's`; `TestUnixEntropy` for the orderings and the pool's movement. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/entropy-syscalls.c`, measured on Linux 6.18.5 (arm64) and Darwin 25.6.0 |
| **`poll(2)`'s event alphabet**: `POLLIN`…`POLLNVAL` are 0x01…0x20 on both. Above them Linux has `POLLRDNORM` 0x40, `POLLRDBAND` 0x80, `POLLWRNORM` 0x100, `POLLWRBAND` 0x200, `POLLMSG` 0x400, `POLLREMOVE` 0x1000, `POLLRDHUP` 0x2000; Darwin has `POLLRDNORM` 0x40, `POLLRDBAND` 0x80, `POLLWRNORM` equal to `POLLOUT`, `POLLWRBAND` 0x100, and the vnode bits `POLLEXTEND` 0x200, `POLLATTRIB` 0x400, `POLLNLINK` 0x800, `POLLWRITE` 0x1000. No request mask fails on either. **Linux** answers all 65536 masks, on every modelled object, as `level & (events \| POLLERR \| POLLHUP)` (`POLLNVAL` alone for a descriptor that is not open, `events = 0` included); `POLLRDNORM` rides with `IN` and `POLLWRNORM` with `OUT` everywhere, `POLLWRBAND` with `OUT` on UDP and on every `AF_UNIX` socket but never on TCP; `POLLREMOVE`, 0x0800, 0x4000 and 0x8000 are never reported (0x8000 not even under `SO_BUSY_POLL`). **Darwin** is not one level masked by the request: a kqueue filter per group of requested bits, so a vnode bit on a socket answers `POLLNVAL`, a reported `HUP` suppresses `OUT`, and a request of 0 reports nothing even for a descriptor that is not open. The model answers Linux and refuses Darwin | `TestPoll`: `every request mask is answered as the measured Linux table says` (the levels as literals, all 65536 masks), `a Darwin-flavoured kernel refuses every poll, whatever it asks`. `TestPollEventsPal` for the PAL's six-row conversions and that `SystemNative_Poll` still answers exactly the six-bit projection. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/poll-alphabet.c`, measured on Linux 6.18.5 (aarch64) and Darwin 25.6.0 on 2026-09-23 |
| **credentials**: `(uid_t)-1` and `(gid_t)-1` are not IDs a process can hold (Linux: `setuid`, `setgid` and a `setgroups` naming one are EINVAL, while `0xFFFFFFFE` is held; Darwin answers EPERM first, so the model refuses them there too). `NGROUPS_MAX` is 65536 on Linux and 16 on Darwin, measured as `setgroups`' own boundary (Darwin checks the count before the privilege). Privilege follows the **effective** uid alone: on Linux a real or saved uid of 0 grants neither `bind` below 1024 nor opening a root-owned 0600 file, and an effective 0 grants both. Which ID a Darwin kernel consults is unmeasured (changing one needs root), so `UnixSystem.withCredentials` refuses Darwin credentials whose real, effective and saved IDs differ | `TestCredentials` (the rows as literals, plus properties over generated credentials); `TestBind`: `a privileged port needs an effective uid of 0`. Probe: `docs/plans/2026-08-23-posix-kernel-extraction/credentials.c`, measured on Linux 6.18.5 (aarch64, root) and Darwin 27.0 (uid 501) on 2026-09-26 |
| **A pipe's buffer**: how much a pipe takes, which writes are short, and when each end polls ready. **Linux** keeps 16 page-sized slots: a write first merges its `count mod page` remainder into the newest slot if it fits after that slot's last byte, then takes whole slots a page at a time; a read frees a slot only when it empties it; the write end is ready iff a slot is free. So capacity depends on write sizes (65520 bytes of 3-byte writes, 45066 of 4097-byte ones), and a write can be taken while the write end is not ready. **Darwin** keeps bytes in a buffer that starts at 512 and grows (1024 … 16384, then 65536: the smallest size strictly above what the write needs) when a write does not fit, and never shrinks; the write end is ready iff `max(16384, size) − held ≥ 512`, so 16000 bytes in a never-grown pipe are not ready and 1000 more make it ready. `PIPE_BUF` is 4096 (the page) and 512: a write of at most that is whole or nothing, a longer one takes what fits. Reads return `min(count, held)` in FIFO order on both | `TestPipeBuffer` (the rules as naive references, property-tested against `PipeBuffer`, and the fill table and readiness rows as literals), `TestPipeBufferCorpus` (every recorded call in `pipeBuffer/{linux,darwin}.txt` replayed), `TestPipeBufferAgainstHost` (random calls against a real pipe on the host: Darwin locally, x86-64 Linux in CI). Probes: `docs/plans/2026-08-23-posix-kernel-extraction/pipe-buffer-{trace,growth,doubling}.c`, `pipe-capacity.c`, and the model checker `pipe-buffer-model.py`, measured 2026-09-26 on Linux 6.18.5 aarch64 (4 KiB pages) and Darwin 27.0.0 arm64; no x86-64 kernel was measured by hand |
| environment entries a real `envp` can hold, and the PAL's view of them | `TestEnvironmentEntryInvariant`: `the entry rule accepts what a real envp can hold`, `the entry rule names what is wrong`; `TestEnvironmentPal`: `a lookup finds what the PAL finds`; `sourcesPure/EnvironmentVariableUnpairedSurrogateName.cs` (differential) |

## The envelope those measurements were taken in

| varied | held fixed |
| --- | --- |
| platform (macOS 26.6 / APFS, Linux 6.x arm64 via `container`; the Darwin model now tracks macOS 27.0, re-checked wherever a host oracle or a compared guest reaches, and `TestDarwinReleaseAgainstHost` fails on any other Darwin major) | one filesystem per platform |
| uid, where it changes the answer | the caller's supplementary groups |
| the flag or argument under test | capabilities — no `CAP_FSETID`, no `CAP_NET_RAW`, unless a row says root |

A claim outside that envelope — a second filesystem, a mount option, a group the
caller is not in, a capability — is **unmeasured**. Measure it and add a row;
do not extrapolate from a neighbouring one.

Note what the host oracles can and cannot reach. They compare against *this*
machine, so each platform's column is falsified only where that platform runs:
macOS locally, Linux in CI. And a privilege-dependent row is checked only at the
privilege the suite happens to run at, which is why the truncation table is
*also* written out as literals in a unit test.

## Facts no test states, because they are about upstream rather than about us

- **`SystemNative_GetFileSystemType` returns 0 for every failure**, not -1. Its
  only managed consumer is `SafeFileHandle.CanLockTheFile`, which refuses a
  *shared* lock under write access on nfs/smb/smb2/cifs — the combination
  `File.WriteAllBytes` asks for. `File.Create` is `FileShare.None` → `LOCK_EX`,
  and never reaches it.
- **`Ext4` cannot be a case.** CoreLib's `UnixFileSystemTypes` has no `ext4`
  member — it is `ext2 = 0xEF53`, with `ext4` commented out as an alias — so the
  managed layer cannot distinguish it.
- **A separate Linux rule strips `S_ISGID` on creation**, when the caller is
  outside the directory's group and lacks `CAP_FSETID`. Unmeasured here and not
  modelled: PawPrint gives every inode `Kernel.GroupId`, so no guest can be
  outside that group. That approximation is what makes
  `CreatingOpenRules.createdPermissions` exact, and it ends when inodes can carry
  a group of their own.
- **The environment is a list, not a map.** A process's environment is a list of
  `name=value` strings; the map every .NET API presents is a *view*, split at each
  entry's first `=`. CoreCLR makes the view total from both ends —
  `GetEnvironmentVariableA` (`pal/src/misc/environ.cpp`) refuses a lookup name
  that is empty or contains `=` *above* its matching loop, and
  `Environment.GetEnvironmentVariables` discards any entry whose first `=` is not
  after the first character. Measured on real .NET via `execve` with a hand-built
  envp: `A=B=C` gives `"A"` = `"B=C"` and `"A=B"` = null; `=C` is invisible to
  both APIs; `DUP=1` with `DUP=2` gives `DUP` = `"1"`. The library stores the
  list as the kernel does (`UnixProcessState.Environment`, NUL-free byte strings
  in `envp` order), and PawPrint's `EnvironmentPal` is the view: first match
  wins, an entry with no `=` is a variable with an empty value, and a lookup name
  is converted to UTF-8 with U+FFFD for each unpaired surrogate before the
  byte-wise comparison (measured on real .NET: `"\uD800"` finds an entry named
  U+FFFD). An empty entry is representable and is a lone NUL in the
  `GetEnvironmentStringsW` block, so every reader that walks the block stops
  there: CoreLib's `GetEnvironmentVariables`, and `CLRConfig::Initialize`, whose
  256-bit Bloom filter of `DOTNET_`/`COMPlus_` knob names then makes a knob set
  only after the empty entry read as unset (unless `DisableConfigCache` is
  non-zero, or a recorded name collides with it). `ClrConfigEnvironment` models
  that exactly; its measured rows are in `TestClrConfigEnvironment`.
